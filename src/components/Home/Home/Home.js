import React from 'react';
// import useAuth from '../../../hooks/useAuth';
import Banner from '../Banner/Banner';

const Home = () => {
    // const { user } = useAuth();
    return (
        <div>
            <Banner></Banner>
        </div>
    );
};

export default Home;